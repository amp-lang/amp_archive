//! Prints a sequence of 1 to 10.
//!
//! Requires linking with the C standard library.

func printf(format: ~const u8, arg: i32) -> i32;

func main() {
    var i: i32 = 0;

    while i < 10 {
        i = i + 1;
        printf("%d\n", i);
    }
}