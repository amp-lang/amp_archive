/// A generic slice type.
export struct Slice {
    ptr: ~mut u8,
    len: uint,
}

/// Returns the length of a slice.
export func SliceLen(slice: Slice) -> uint {
    return slice.len;
}

/// Converts a slice to a pointer.
export func SliceAsPtr(slice: Slice) -> ~mut u8 {
    return slice.ptr;
}