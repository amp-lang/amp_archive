import "Std";

export func Main() {
    var buffer = Std.Buffer();
    Std.BufferWriteBytes(~mut buffer, "Hello, world!\n");
    Std.Print(Std.BufferFlushToSlice(~mut buffer));
    Std.BufferFree(~mut buffer);
}