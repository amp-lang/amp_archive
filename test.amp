struct MyStruct {
    member: i32,
}

func printf(format: ~const u8, value: i32) -> i32;

func Main() {
    var my_struct = MyStruct .{ member = 42 };
    printf("Hello, world! %d", my_struct.member % 2);
}