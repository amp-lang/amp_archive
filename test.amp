import "Std";

export func Main() {
    var hello_world = Std.Alloc(14);
    Std.Copy(hello_world, "Hello, world!\n");

    var i = 0 as uint;
    while i < Std.SliceLen(hello_world) {
        Std.PrintAscii(hello_world[i]);
        i = i + 1;
    }
}